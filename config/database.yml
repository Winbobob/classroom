development: &default
  adapter: postgresql
  encoding: unicode
  database: classroom_development
  pool: <%= ENV.fetch('RAILS_MAX_THREADS', 1) %>
  username: postgres
  password:
  host: localhost

test:
  <<: *default
  database: classroom_test
